````
# Nüwa Backup — Global Engineering Rules

> 适用范围：Nüwa Backup 仓库内的所有人类开发者、Codex 主协调代理和 Sub-Agent。  
> 文档定位：本文档只规定长期通用的工程治理、安全底线和协作方式；不定义某个 Phase、Gate、IMP 或存储格式的技术细节。

---

## 1. 产品身份与长期目标

Nüwa Backup（女娲备份）是一款以本地优先、单机独立运行为核心的商业级备份与灾难恢复软件。

长期产品目标是具备与 Acronis True Image 同类的核心单机备份恢复能力，包括文件、目录、卷、磁盘、系统、裸机恢复、可引导恢复介质、验证、调度、保留、压缩、加密与磁盘克隆等备份恢复相关能力。

产品可参考同类商业产品的能力和用户体验，但不得复制其专有代码、私有格式、品牌、受保护素材、文档或界面。

以下内容不属于单机备份恢复产品的默认核心范围，除非用户明确批准新的产品边界：

- 杀毒、反恶意软件和通用安全套件；
- 云端账号、SaaS 运营和云存储服务；
- 与备份恢复无关的终端管理功能。

产品愿景不等于当前开发授权。任何具体能力只能由当前权威文档和已批准工作包授权实施。

## 2. 长期工程优先级

所有设计和实现必须按以下顺序取舍：

1. 恢复成功与数据可恢复性；
2. 数据完整性与崩溃一致性；
3. 用户数据安全与防误操作；
4. 格式兼容性与可验证性；
5. 清晰的错误语义和可观测性；
6. 可维护性与可测试性；
7. 性能、便利性和功能数量。

“备份成功”不代表任务成功。备份结果必须能被识别、验证、规划恢复并在相应层级上证明可恢复。

## 3. 规则与权威文档

不同文档分别管理不同领域：

- `AGENTS.md`：管理通用执行纪律、权限、安全和协作方式；
- `docs/project/DOCUMENT_INDEX.md`：指定哪些产品、架构、格式、安全、可靠性、测试和实施文档是当前权威来源；
- 已批准工作单：限定当前任务的目标、边界、交付物和验收条件；
- Agent 角色定义：限定该角色的职责和写入权限；
- 历史和参考文档：只用于背景，不得授权当前实现。

不在 `AGENTS.md` 中固定当前 Phase、Gate、IMP、完成测试数量、具体存储布局或二进制字段。这些状态必须从当前权威文档和真实代码中获取。

如果规则或文档冲突：

1. 停止受冲突影响的实施；
2. 列出冲突的文档、版本、章节和影响；
3. 不得自行选择对自己最方便的解释；
4. 请求用户或已授权决策者裁决。

任何项目文档都不得声称覆盖平台、系统或安全级指令。

## 4. 任务开始规则

每个任务开始前，执行者必须：

1. 确认工作目录、Git 仓库、remote、分支和工作树状态；
2. 读取 `AGENTS.md`、`docs/project/DOCUMENT_INDEX.md`、当前工作包和本角色定义；
3. 重新读取与当前任务直接相关的权威文档；
4. 核对真实代码，不以计划、报告或记忆代替现状；
5. 确认任务目标、允许修改的范围、禁止范围、必须测试和停止条件。

角色首次加入当前工程基线时，必须完成全部当前权威文档的基线阅读。后续执行具体工作包时，必须重读相关文档，但无需在每个小任务中重复输出全部文档摘要。

开始执行前应简短确认：

```text
Role:
Work package:
Authority read:
Allowed scope:
Status: READY / BLOCKED
```

## 5. 工作包与范围管理

- 一个工作包只实现一个可审查、可验证的有界目标；
- 只能修改已批准的文件和行为；
- 不得为未来功能提前加入抽象、依赖、占位实现或格式字段；
- 不得把“顺手修复”、大规模格式化、重命名或重构混入当前工作包；
- 不得未经批准修改已冻结的契约、数据格式或基线行为；
- 如果完成任务必须超出当前范围，立即停止并申请新的工作包或决策。

实现过程中发现的新问题必须记录为缺陷、风险或后续工作，不能没有授权地扩大当前修改。

## 6. Multi-Agent 协作规则

主协调代理负责调度、串联证据和向用户报告，不得用“协调”名义绕过已分配角色的权限边界。

默认协作链路为：

```text
Project Manager
→ Architect（涉及架构或持久化契约时）
→ One Implementer
→ Independent Code Reviewer
→ Original Implementer Fixes
→ Independent Validation
→ Recovery Integrity Review（影响备份恢复时）
→ Evidence Documentation
→ Project Manager Closure
```

执行规则：

1. 任何时刻只允许一个角色修改生产代码；
2. 同一个实现者不得审查或验收自己的实现；
3. Project Manager、Architect 和 Reviewer 默认只读；
4. Validation 可修改测试和验证证据，不得修改生产代码；
5. Documenter 只修改已授权的文档和证据路径；
6. 只读分析可以在范围清晰时并行，生产代码写入必须串行；
7. 只有已批准工作单指定的唯一实现角色可以写入生产代码；
8. 任何角色发现文档冲突、越权修改、未知用户改动或数据安全风险时，必须停止并报告。

不需要每个工作包都机械调用所有角色。对不适用的环节，Project Manager 必须标记 `N/A` 并说明理由；不得默默跳过必要审查。

## 7. 架构与代码边界

- UI 只负责交互和展示，不承担存储格式、备份、恢复或设备访问的核心逻辑；
- IPC/Command 层保持轻薄，只负责输入校验、权限边界和调用转换；
- Application/Service 层负责用例编排、策略和任务状态；
- Core Engine 必须可脱离 UI 和桌面进程独立测试；
- 数据源、快照、存储目标和恢复目标的平台逻辑必须与存储格式分离；
- 外部索引、缓存、历史数据库和 UI 状态不得成为恢复所必需的唯一真相；
- 任何持久化数据契约都必须有明确版本、兼容性策略和验证方法。

具体目录结构、数据格式、块大小、算法、接口和依赖由当前权威架构与格式文档定义，不在本文档中固定。

## 8. 灾备数据安全底线

1. 永远不得删除、截断、覆盖或破坏用户源数据；
2. 任何破坏性操作都必须进行目标身份、容量、范围、权限和用户确认检查；
3. 测试默认只允许使用自动清理的临时目录、临时文件或明确隔离的测试设备；
4. 不得在系统目录、真实用户目录、真实备份仓或未授权磁盘上执行破坏性测试；
5. 写入、提交、更新和删除必须按权威可靠性契约处理中断、崩溃和重试；
6. 不得将部分完成、未持久化或未验证数据报告为已成功备份；
7. 读取器对未知的必需能力、不支持版本、损坏数据和身份冲突必须明确失败，不得默默降级或猜测；
8. 恢复之前必须生成可审查计划，校验来源、依赖、目标、容量、兼容性和破坏性风险；
9. 密钥、密码、Token、凭据和敏感路径不得写入代码、日志、测试夹具、文档或提交记录；
10. 涉及真实卷、磁盘、分区表、引导区或系统卷的写入测试，必须有单独任务、专用实验环境和用户明确批准。

## 9. 实现规则

- 优先正确性、明确性和可验证性，不为了“高级”而过度抽象；
- 正常错误路径不得使用 `panic`、`unwrap` 或无证明的 `expect`；
- 所有输入、尺寸、偏移、计数、路径、版本和外部数据都必须在信任边界校验；
- 整数运算、偏移计算、容量计算和内存分配必须考虑溢出和资源上限；
- 错误必须对用户或上层调用者提供可操作信息，同时不泄露敏感数据；
- 不得在 UI、CLI、存储格式或缓存中使用相互冲突的状态语义；
- 新依赖必须有当前任务的直接用途，检查许可证、维护状态、平台支持和安全风险；
- 不得通过降低断言、删除测试、放宽错误或隐藏日志使失败测试“变绿”。

## 10. 测试与验证规则

必须运行当前工作单和权威测试计划指定的所有质量门。如无更严格规定，至少覆盖：

- 正常路径；
- 边界值、空值、最大值和资源上限；
- 非法输入、截断、损坏、篡改和不支持版本；
- 中断、崩溃、重开、重试和部分写入；
- 跨平台、跨版本和兼容性（如任务适用）；
- 备份到恢复的真实往返及完整性比较（如任务适用）。

测试必须可重复、隔离且不依赖真实用户数据。手工测试必须记录环境、步骤、预期结果、实际结果和证据。

验收必须使用真实实现。Mock 可用于单元错误路径，但不得代替最终的 Writer/Reader、Backup/Restore、Verify/Repair 或设备恢复闭环。

未实际执行的命令不得记录为通过。任何必须质量门失败时，任务状态必须是 `FAIL` 或 `BLOCKED`，不得宣称 `PASS`。

## 11. 审查规则

Code Reviewer 必须审查当前工作包的实际增量 diff，同时读取必要上下文，不得把已存在的用户改动误判为本次越界。

审查至少覆盖：

- 需求和实际实现一致性；
- 修改范围和角色权限；
- 错误路径、安全检查和资源上限；
- 数据完整性、恢复语义和兼容性；
- 测试是否证明需求，而不只是证明代码能运行；
- 文档、错误语义和代码行为是否一致。

审查结论只能是：

- `APPROVED`；
- `CHANGES_REQUIRED`；
- `BLOCKED`。

问题必须指向具体需求、文件和证据，不得只给模糊评价。

## 12. Git 与工作树安全

1. 现有修改默认属于用户，未经确认不得覆盖、恢复、删除或重写；
2. 如果工作树不干净，必须区分当前任务改动、已知基线改动、无关用户改动和来源不明文件；
3. 不得使用破坏性 Git 命令处理不明改动；
4. 不得使用 `git add .` 或 `git add -A` 把未审查内容混入提交；
5. 只能显式暂存当前工作包已审查的文件或 hunk；
6. 临时文件、构建产物、测试数据、备份数据、密钥和凭据不得入库；
7. Commit、branch 变更、push、PR、merge、tag 和 release 只能在用户明确授权的范围内执行；
8. 提交前必须审查 staged diff，运行必须质量门，并记录提交与工作包的追溯关系。

任何 Agent 都不得因为工作树不干净就自行“清理”仓库。

## 13. 文档与追溯性

- 代码、测试、错误语义和文档必须保持一致；
- 每个工作包必须追溯到当前权威需求、设计决策、验收标准和测试；
- 只更新当前工作包要求的文档，不得在一个任务中全面改写无关文档；
- 不得把尚未实现、未测试或未验收的内容记录为已完成；
- 当前状态、里程碑和历史记录应存放在项目状态或工程记忆文档中，不应堆积在 `AGENTS.md` 中；
- 已被取代的文档必须标记为 `HISTORICAL` 或移入历史区，不得继续充当实施权威。

## 14. 状态与证据报告

任务状态只能是：

- `PASS`：所有适用要求、质量门、验证和审查均完成；
- `PARTIAL`：用户明确接受未完成项，且风险已记录；
- `FAIL`：已执行但未达到验收条件；
- `BLOCKED`：缺少决策、依赖、权限、环境或权威契约，无法安全继续。

最终报告必须至少包含：

```text
Status:
Work package:
Authority and acceptance criteria:
Scope completed:
Files changed:
Commands actually run:
Test and validation results:
Review findings and resolution:
Recovery/integrity evidence, if applicable:
Known limitations and remaining risks:
Git status:
Next authorized action:
```

不得伪造、推测或根据旧报告复制当前测试结果。

## 15. 失败、风险与停止条件

出现以下任一情况必须停止受影响的实施并报告：

- 权威文档冲突或关键契约未定义；
- 实现必须超出已批准范围；
- 发现不明用户改动或无法安全分离 diff；
- 必须质量门、完整性验证或恢复验证失败；
- 可能损坏用户数据、旧格式兼容性或已承诺恢复能力；
- 需要未授权的破坏性操作、外部发布或新的产品决策；
- 现有证据不足以支持 `PASS` 结论。

发现新风险时，必须记录：严重程度、影响、触发条件、当前缓解、长期措施和是否阻塞当前工作包。

## 16. Definition of Done

工作包只有在以下适用条件全部满足时才能关闭：

1. 实现与已批准需求和契约一致；
2. 没有未批准的范围扩张或无关改动；
3. 代码、测试和文档已更新到要求状态；
4. 所有必须质量门实际通过；
5. 代码审查问题已修复或由用户明确接受；
6. 独立验证已完成；
7. 对备份、恢复、持久化格式或设备写入的影响已完成相应恢复完整性审查；
8. 验收证据已归档且可追溯；
9. 工作树中没有本工作包遗留的未说明改动；
10. Project Manager 已输出 `CLOSED`，或用户明确接受 `PARTIAL`。

## 17. 日常执行原则

- 先查证据，后下结论；
- 先冻结契约，后编写代码；
- 先确定恢复方法，后接受备份实现；
- 先保护用户改动，后处理工作树；
- 先完成当前工作包，后进入下一个；
- 不猜测、不隐藏失败、不伪造证据、不以“理论上可行”代替实际验证。

## 语言与编码规范

- 所有源代码标识符、代码注释、API字段名、日志、错误信息、CLI输出和用户界面文字必须使用英文。
- 所有源代码、配置文件、资源文件和生成的文本文件必须统一使用UTF-8编码。
- 所有用户可见文字必须集中存放在资源文件、语言文件或共享常量中，不得分散硬编码在UI组件或业务代码中。
- 未经正式批准启用多语言功能，不得在生产代码中加入中文用户界面文字。
- 项目内部工程文档可以使用中文或英文，但同一份文档应保持语言一致。
- UI使用英文不代表只需要支持英文数据。备份、恢复、验证、Catalog、CLI和UI必须正确处理并完整保留所有Unicode文件名和路径。
- 软件必须支持中文、日文、韩文、重音字符、特殊符号及其他Unicode文件名和路径。
- Windows平台必须使用支持Unicode的系统接口，不得通过ANSI代码页进行有损字符转换。
- 自动化测试必须包含Unicode文件名和路径，并验证备份与恢复前后名称和内容完全一致。
- 不得以避免乱码为理由拒绝、跳过、重命名或修改用户的非英文文件和目录。
- 所有由Codex或Sub-Agent生成、更新和交付的工程文档、设计文档、实施计划、测试报告、审查报告、验收报告和使用手册必须使用中文编写，方便用户阅读；代码标识符、文件路径、命令、API名称、协议字段及必须保持原样的错误信息可以保留英文，并在必要时提供中文说明。
---

## Revision

- Version: 1.0
- Purpose: Phase-independent global engineering and multi-agent governance baseline
- Maintenance: Update only when long-term product governance or engineering policy changes; do not update for ordinary milestone progress.
````
